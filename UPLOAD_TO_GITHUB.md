# Загрузка

```powershell
cd D:\git\DadwayVPN-Windows
git add --all
git commit -m "Dadway VPN Windows v2.0.1"
git push origin main
```

После push откройте Actions и скачайте артефакт `DadwayVPN-Windows-v2.0.1`.
